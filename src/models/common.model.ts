export type SocialMedia = {
	name: string;
	links: string;
};

export type Reviews = {
	user_id: string;
	rating: number;
	review: string;
};

export type ServicePackages = {
	name: string;
	price: number;
	description: string;
};
